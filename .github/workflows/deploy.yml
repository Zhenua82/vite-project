# name: Build and Deploy to GitHub Pages

# on:
#   push:
#     branches:
#       - master  # или ваша основная ветка (например, main)

# permissions:
#   contents: write
#   pages: write

# jobs:
#   build-and-deploy:
#     name: Build and Deploy
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '19'  # или любая нужная версия

#       - name: Install dependencies
#         run: npm install

#       - name: Build project
#         run: npm run build

#       - name: Deploy to GitHub Pages
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: dist
#           publish_branch: gh-pages  # ветка для публикации GitHub Pages



# name: Build and Deploy to GitHub Pages

# on:
#   push:
#     branches:
#       - master 

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '19'

#       - name: Install dependencies
#         run: npm install

#       - name: Build
#         run: npm run build

#       - uses: actions/upload-pages-artifact@v3
#         with:
#           path: dist
#       - uses: actions/deploy-pages@v4

name: Deploy Vite React App to GitHub Pages

on:
  push:
    branches: [ master ]   # или укажи ветку, из которой надо деплоить
  workflow_dispatch:      # возможность запускать вручную

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

    # Установка Node
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

    # Установка зависимостей
      - name: Install dependencies
        run: npm ci

    # Сборка Vite
      - name: Build project
        run: npm run build

    # Загрузка сборки как артефакт Pages
      - name: Upload production-ready build files
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4
